name: Build Android APK
on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Initialize Project and Install Capacitor
        run: |
          # إنشاء ملف package.json إذا لم يكن موجوداً
          npm init -y
          # تثبيت الأدوات اللازمة
          npm install @capacitor/core @capacitor/cli @capacitor/android

      - name: Create Web Assets and Config
        run: |
          mkdir -p www
          # التأكد من وجود ملف index.html
          if [ ! -f index.html ]; then
            echo "<html><body><h1 style='font-size:100px;'>🖕🏿</h1></body></html>" > index.html
          fi
          cp index.html www/
          # تهيئة كاباسيتور
          npx cap init "EmojiApp" "com.emoji.express" --web-dir www
          npx cap add android

      - name: Build Android APK
        run: |
          npx cap copy
          cd android && ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Emoji-App-Debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
